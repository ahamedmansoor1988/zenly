import { SettingsView } from "./SettingsView";

export const metadata = {
  title: "Zenly Settings",
  description: "Manage your account, connected integrations, and sign out.",
};

export default function SettingsPage() {
  return <SettingsView />;
}
